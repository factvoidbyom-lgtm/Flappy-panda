import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.omrahman.flappypanda.remastered',
  appName: 'FLAPPY PANDA : Remastered',
  webDir: 'dist'
};

export default config;
